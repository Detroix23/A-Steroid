# A-Steroid.
Game made with `pyxel`, training the _Nuit du C0de_.

**Costraints**.
- Python version 3.11.
- Write everything in one single file.
- One single `.pyxres` file.
- Docstrings in French.
- Must use a tilemap, a music track, the `pyxel` camera. 